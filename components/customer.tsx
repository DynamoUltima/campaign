import { Tab } from "@headlessui/react";

const Customer = () => {
    return (
        <Tab.Panel className="flex bg-white h-full">
            <div>Customer</div>
        </Tab.Panel>
    );
}

export default Customer;